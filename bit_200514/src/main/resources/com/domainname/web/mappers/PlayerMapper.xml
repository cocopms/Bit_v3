<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.domainname.web.mappers.PlayerMapper">
    <select id="selectAll" resultType="com.domainname.web.domains.PlayerDTO">
        select
         player_id playerId,
         player_name playerName,
         team_id teamId,
         nickname,
         e_player_name ePlayerName,
         join_yyyy joinYyyy,
         position,
         back_no backNo,
         nation,
         birth_date birthDate,
         solar,
         height,
         weight
         from player
    </select>
</mapper>